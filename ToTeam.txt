How to build:

    download node.js

    type these in terminal:
        pip install npm
        npm install express
        npm install -g nodemon
        npm install express

    then run it:
        nodemon app.js